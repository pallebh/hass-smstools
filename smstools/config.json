{
    "name": "Sms gateway(smstools)",
    "version": "1.0",
    "slug": "Sms gateway",
    "description": "",
    "startup": "before",
    "boot": "auto",
    "ports": {},

    "options": {
        "smstools": {
            "device": "/dev/ttyACM0",
            "pin": "",
            "baudrate": "19200"
        }
    },

    "schema": {
        "smstools": {
            "device": "str",
            "pin": "str",
            "baudrate": "int"
        }
    },
    "image": "smstools/{arch}-addon-smstools"
}
