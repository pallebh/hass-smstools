{
    "name": "sms gateway(smstools)",
    "version": "1.0",
    "slug": "sms gateway",
    "description": "",
    "startup": "before",
    "boot": "auto",
    "ports": {},

    "options": {
        "smstools": {
            "device": "/dev/ttyACM0",
            "pin": "",
            "baudrate": "19200"
        }
    },

    "schema": {
        "smstools": {
            "device": "str",
            "pin": "str",
            "baudrate": "int"
        }
    }
}
